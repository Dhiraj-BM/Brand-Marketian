# Copy to .env for local development. In production, set these as real
# environment variables on the host (Render, etc.) — never commit a real .env.

PORT=4000
MONGODB_URI=mongodb://127.0.0.1:27017/brandmarketian

# Required in production: a long random string. If unset, the server falls
# back to a hardcoded dev secret, which is unsafe to run publicly.
JWT_SECRET=

# Comma-separated list of allowed origins, e.g.:
# https://brandmarketian.com,https://admin.brandmarketian.com
CORS_ORIGIN=*

# SMTP (lead notification emails)
SMTP_HOST=
SMTP_PORT=465
SMTP_USER=
SMTP_PASS=
LEAD_NOTIFY_TO=growth@brandmarketian.com

# WhatsApp lead notifications (optional)
WA_PHONE_ID=
WA_TOKEN=
WA_NOTIFY_TO=

# First admin user, created by `npm run seed`.
# Required in production: change the password from the default.
ADMIN_EMAIL=growth@brandmarketian.com
ADMIN_PASSWORD=

# Influencer/creator lookup provider:
#   'sample'      no network — realistic placeholder data (default)
#   'instagrapi'  REAL data via the self-hosted Python service in server/instagram-svc
#                 (needs a throwaway IG account; see that folder's README)
#   'scraperapi2' RapidAPI "Instagram Scraper API 2" (SocialAPI) — needs RAPIDAPI_KEY
#   'rapidapi'    a RapidAPI listing you point at with RAPIDAPI_KEY + RAPIDAPI_HOST
#   'modash'      Modash Influencer Report API — needs MODASH_API_KEY (paid)
CREATOR_PROVIDER=sample

# provider=instagrapi
INSTAGRAM_SVC_URL=http://127.0.0.1:8000
INSTAGRAM_SVC_TOKEN=

# provider=modash
MODASH_API_KEY=

# provider=rapidapi / scraperapi2
RAPIDAPI_KEY=
RAPIDAPI_HOST=

CREATOR_CACHE_TTL_MS=86400000
